@model IEnumerable<OurNewProject.Models.Product>

@{
    ViewData["Title"] = "Menu";
}


<head>
    <link rel="stylesheet" href="~/css/Menu.css">

</head>
@*<br />
    <div class="centersearch">
        <h2 class="ourTitle">Our</h2>
        <h1 class="menuTitle">MENU</h1>

        <form asp-action="Search">
            <input name="queryN" placeholder="Search" class="searchbar" />
            <input type="submit" value="Search" class="searchButton" />
        </form>
    </div>*@




@*<tbody>
    @foreach (var item in Model)
    {
        <div class="tab-pane fade show active" id="breakfast" role="tabpanel" aria-labelledby="breakfast-tab">
            <div class="row">
                <div class="col-md-6">
                    <div class="single_menu">

                        <img src="https://i.imgur.com/kbpceNv.jpg" alt="burger">
                        <div class="menu_content">
                            <h4>    @Html.DisplayFor(modelItem => item.Name)  <span> @Html.DisplayFor(modelItem => item.Price)</span></h4>
                            <p> @Html.DisplayFor(modelItem => item.Description)</p>
                        </div>

                    </div>
                </div>
            </div>
        </div>

    }
</tbody>*@




@*<section id="our_menu" class="pt-5 pb-5">*@
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="page_title text-center mb-4">
                    <h1>our menu</h1>
                    <div class="single_line"></div>
                </div>
            </div>
        </div>

  @*      <div class="row">
            <ul class="nav nav-tabs menu_tab mb-4" id="myTab" role="tablist">
                <li class="nav-item">
                    <a class="nav-link active" id="breakfast-tab" data-toggle="tab" href="#breakfast" role="tab">גלידות</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" id="lunch-tab" data-toggle="tab" href="#lunch" role="tab">תוספות</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" id="dinner-tab" data-toggle="tab" href="#dinner" role="tab">Dinner</a>
                </li>
            </ul>
        </div>*@

        @foreach (var item in Model)
        {
            
    <div class="tab-pane fade show active" role="tabpanel">
        <div class="row">
            <div class="col-md-8">
                <div class="single_menu">
                    <img src="https://did.li/PFMw5">
                    <div class="menu_content">
                        <h4>    @Html.DisplayFor(modelItem => item.Name)  <span> @Html.DisplayFor(modelItem => item.Price)</span></h4>
                        <p> @Html.DisplayFor(modelItem => item.Description)</p>
                    </div>
                </div>
            </div>

        </div>
        <button>add to cart</button>
    </div>

                
            
         @* if (item.Category.Name.Equals("תוספות"))
            {
                <div class="tab-pane fade" id="dinner" role="tabpanel" aria-labelledby="dinner-tab">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="single_menu">
                                <img src="https://i.imgur.com/kbpceNv.jpg" alt="burger">
                                <div class="menu_content">
                                    <h4>    @Html.DisplayFor(modelItem => item.Name)  <span> @Html.DisplayFor(modelItem => item.Price)</span></h4>
                                    <p> @Html.DisplayFor(modelItem => item.Description)</p>
                                </div>

                            </div>
                        </div>
                    </div>
                </div>

            }*@


        }



    </div>
@*</section>*@



